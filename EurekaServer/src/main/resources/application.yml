
#Configring Eureka Server For Application
spring:
  application:
    name: eureka-service
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761